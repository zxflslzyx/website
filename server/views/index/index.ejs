<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <style>
        *{
            margin: 0;
            padding: 0;
        }
        html,body{
            width: 100%;
            min-height: 100%;
            font-size: 13.333vw;
        }
        .link_wrap{
            display: block;
            min-height: 100vh;
            width: 100%;
            background: #ccc;
            padding: 1px;
        }

        .link_wrap .link_item{
            display: block;
            margin: .2rem .5rem;
            padding: .2rem 0;
            color: white;
            font-size: .4rem;
            background: blue;
            text-decoration: none;
            text-align: center;
            border-radius: .1rem;
        }
    </style>
</head>
<body>
<aside class="link_wrap">
    <% for(let i=0;i<data.length;i++) {%>
        
    <a href="<%= data[i].url %>" class="link_item">
        <%= data[i].name %>
    </a>
    <%}%>
</aside>
</body>
</html>